<?php

// try
// {
// 	// On se connecte à MySQL
// 	$bdd = new PDO('mysql:host=localhost;dbname=weatherapp;charset=utf8', 'root', '');
// }
// catch(Exception $e)
// {
// 	// En cas d'erreur, on affiche un message et on arrête tout
//         die('Erreur : '.$e->getMessage());
// }
// $resultat = $bdd->query('SELECT * FROM météo');

// $donnees = $resultat->fetch();

// while ($donnees = $resultat->fetch())
{
  // echo $donnees['ville'];
  // echo "<br>";
  // echo $donnees['haut'];
  // echo "<br>";
  // echo $donnees['bas'];
  // // echo "<br>";
  // echo 'la température la plus haute de :' . $donnees ['ville']. 'est de' .$donnees['haut']. ' et la plus basse est de' .$donnees['bas'].'</br>';
  // echo 'ville  haut  bas </br>';
  // echo $donnees ['ville']  .$donnees['haut'] .$donnees['bas'];
  // echo '<td>';
}

// $resultat->closeCursor();
 ?>
 <?php
//   $table = 'météo';
//   $resultat = 'SELECT * FROM météo';
// $resultat = 0;
// while ($resultat <20)
// {
//   echo '<td>';
//   $resultat++;
//   // $resultat ->closeCursor(); 
// }
 
 ?>